import { createTheme } from '@material-ui/core';

const theme = createTheme({
  palette: {
    primary: {
      main: "#DC602E"
    },
    secondary: {
      main: "#F0544F"
    }
  }
});

export default theme;